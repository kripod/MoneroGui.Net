<UserControl x:Class="Jojatekok.MoneroGUI.Views.OptionsWindow.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" x:Name="CheckBoxStartableOnSystemLogin" Content="{Binding Path=OptionsGeneralIsStartableOnSystemLogin, Source={StaticResource Resources}}" Margin="0 0 0 5"/>
        <CheckBox Grid.Row="1" x:Name="CheckBoxSafeShutdownEnabled" Content="{Binding Path=OptionsGeneralIsSafeShutdownEnabled, Source={StaticResource Resources}}"/>
    </Grid>
</UserControl>
