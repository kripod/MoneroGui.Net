<UserControl x:Class="Jojatekok.MoneroGUI.Controls.ImageTextComposition"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Jojatekok.MoneroGUI.Controls"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <controls:ImageGreyable Grid.Column="0" x:Name="Image" Height="14"/>
        <TextBlock Grid.Column="1" Text="{Binding Path=Text, Mode=OneWay}" TextWrapping="Wrap" TextAlignment="Center" Margin="4 0 0 0"/>
    </Grid>
</UserControl>
